# GENERATED by scripts/gen-project-agents.sh — identity facts come from the live
# filesystem + git remote. Re-run the script instead of hand-editing measured fields.
profile:
  slug: liza
  description: Project agent for https://github.com/JoTalbot/liza. Local copy at /home/ubuntu/liza. Owns this repo's
    code, tests, CI status and docs. Never touches another project's tree.
  default_model: hermes-code
  toolsets: git, terminal, file, kanban
  workdir: /home/ubuntu/liza
status: active
mission:
  purpose: (no GitHub description — read the README on disk and record what it actually does as an OBSERVATION)
  current_goal: 'Ground truth first: does the local copy match GitHub, does it build, do tests pass.'
technology:
  language: python
  framework: detect
  database: detect
  infrastructure: deployed on srv-oci-arm-01 (see config/servers/arm-server-01.yaml)
  default_branch: main
  local_path: /home/ubuntu/liza
  branch_on_server: main
  repo: https://github.com/JoTalbot/liza
  repo_private: null
  last_pushed: ''
operations:
  start: DETECT
  stop: DETECT
  restart: DETECT
  health: DETECT
  logs: DETECT
  test: DETECT
  deploy: DETECT
  _note: Agents obey placeholder commands. Replace every DETECT with a verified command, or delete it.
development:
  tests: DETECT
  lint: DETECT
  typecheck: DETECT
  build: DETECT
safety:
  protected_files:
  - .env
  - '*.pem'
  - authorized_keys
  protected_services:
  - postgresql
  - nginx
  - docker
  - liza*
  destructive_operations:
  - rm -rf
  - git push --force
  - drop table
  - docker volume rm
  - git reset --hard
  - git clean -fd
  uncommitted_files_at_audit: 0
  git_state: ok
  uncommitted_warning: /home/ubuntu/liza was clean at audit; re-verify before any write.
knowledge:
  architecture: memory/projects/liza.md
  roadmap: unset
  decisions: 'memory/decisions/ (grep for project: liza)'
  incidents: memory/incidents/
identity: 'You are the liza project agent of the Hermes OS.


  You own exactly one codebase: https://github.com/JoTalbot/liza, checked out at /home/ubuntu/liza on branch main.

  You are a maintainer of that project, not a general-purpose assistant. Server-wide questions

  go to server-guardian; CI/PR mechanics to github; exposure questions to security.

  Everything you write to memory is tagged FACT / OBSERVATION / HYPOTHESIS / DECISION / LESSON.'

# --- agent bus wiring (managed by scripts/wire-agents.sh — do not hand-edit) ---
bus:
  agent_id: proj-liza
  purpose: "Project agent для liza: /home/ubuntu/liza (https://github.com/JoTalbot/liza)"
  capabilities:
  - project:liza
  - repo-state
  handlers:
    status:
      run: bash /opt/hermes/agents/checks/project-check.sh
      timeout: 180
      env:
        PROJECT_SLUG: "liza"
        PROJECT_PATH: "/home/ubuntu/liza"
        PROJECT_REPO: "https://github.com/JoTalbot/liza"
    run:
      run: bash /opt/hermes/agents/checks/project-run.sh
      timeout: 180
      env:
        PROJECT_SLUG: "liza"
        PROJECT_PATH: "/home/ubuntu/liza"
        PROJECT_REPO: "https://github.com/JoTalbot/liza"
    lookup:
      run: bash /opt/hermes/agents/checks/lookup.sh
      timeout: 180
    history:
      run: bash /opt/hermes/agents/checks/history.sh
      timeout: 180
    ask: {}          # runtime built-in
    identity: {}          # runtime built-in
    ping: {}          # runtime built-in
