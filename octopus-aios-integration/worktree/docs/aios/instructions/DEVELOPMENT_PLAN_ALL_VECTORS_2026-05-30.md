# Octopus — План развития по ВСЕМ векторам (2026-05-30)
Приоритет (инструкция #11): ПАМЯТЬ > ЖИТЬ > УПРОЩЕНИЕ > СОСУЩЕСТВОВАНИЕ > РАЗМНОЖАТЬСЯ/РАЗВИВАТЬСЯ/УЧИТЬСЯ/МЕНЯТЬСЯ.
Глобальная цель (инструкция #05): ПРОЦЕСС.

## Сделано в этой итерации (база для всего остального)
- SLO зелёный (smoke 37/0/0). Child auth починен. Registry truthful. Ollama не перегружает CPU.

---
## 8. ПАМЯТЬ (приоритет №1)
Цель: бессмертная, восстановимая, унифицированная память.
- [x] (2026-05-30) Авто-проверка инварианта 2 копий: audit + timer + smoke check 27 (coverage 1.0) для каждого sha256-объекта
      (local pool + хотя бы один из: IPFS / Garage S3 / AWS HTTP). Отчёт в smoke.
- [x] Регулярный restore-drill уже есть — расширить: рандомная выборка 5 объектов/сутки,
      сверка sha256 после восстановления из КАЖДОГО backend независимо.
- [x] Единый канонический путь: ref -> sha256 -> {pool, ipfs, s3}. Убрать legacy Pingvin полностью.

## 2/ЖИТЬ (приоритет №2)
Цель: 0 ложных алертов, 0 restart-loop, контролируемый load.
- [x] (2026-05-30) ollama MAX_LOADED_MODELS=1 -> load 8.8->~5; наблюдать дальше; цель < 4.0 устойчиво. Если нет — вынести embeddings
      на отдельный keep-alive или урезать частоту dev-agent loop.
- [x] (2026-05-30) smoke checks 28-29: load average + MemAvailable добавлены
- [x] Авто-фиксация роста NRestarts>20 в лог обслуживания (инструкция #07).

## 7/УПРОЩЕНИЕ (приоритет №3)
Цель: меньше путей, меньше дублей.
- [x] Свести запуск детей к ОДНОМУ пути (systemd unit). spawn_endpoint и autoscale должны
      вызывать systemctl enable --now octopus-child@N, а не bare docker run (источник бага 2026-05-30).
- [x] Удалить дублирующие code-patch монтирования, где образ уже содержит свежий код.
- [x] Один источник истины конфигурации: только /run/octopus/config.yaml (генерируемый).

## 6/СОСУЩЕСТВОВАНИЕ (приоритет №4)
Цель: жить рядом с другими сервисами без конфликтов.
- [x] Аудит открытых портов на main и aws-node (только loopback/туннель, кроме нужного).
- [x] Лимиты CPU/RAM на все контейнеры (у детей уже есть; проверить master и сервисы).

## РАЗМНОЖАТЬСЯ (требует явной команды по каждому ресурсу — #08/#09)
- [ ] (ТОЛЬКО ПО КОМАНДЕ) Проверить AWS Free Tier-ноду aws-node-eu-1: статус, стоимость, реплика памяти.
- [ ] (ТОЛЬКО ПО КОМАНДЕ) Любое новое внешнее размещение — сначала предложение пользователю.
- НЕ запускать автономный неконтролируемый цикл саморазмножения.

## РАЗВИВАТЬСЯ / УЧИТЬСЯ / МЕНЯТЬСЯ
- [x] Dev-agent loop: снизить частоту/контекст, чтобы не упирался в LLM timeout (см. ЖИТЬ).
- [x] GraphRAG: индексировать сегодняшний experience-файл (урок про child auth).
- [x] Wiki: авто-страница "Child node lifecycle & auth" из сегодняшнего опыта.

## Правило безопасности (зафиксировано)
Никаких необратимых автономных действий (внешнее размножение, массовое саморедактирование,
использование платёжных кредов для создания ресурсов) без пошагового подтверждения пользователя.
