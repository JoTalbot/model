# Octopus — РОАДМАП по всем векторам (2026-05-30 v4)
Составлен агентом Arena. Приоритет (#11): ПАМЯТЬ > ЖИТЬ > УПРОЩЕНИЕ > СОСУЩЕСТВОВАНИЕ > РАЗМНОЖАТЬСЯ/РАЗВИВАТЬСЯ/УЧИТЬСЯ/МЕНЯТЬСЯ.
Глобальная цель (#05): ПРОЦЕСС. Ограничение безопасности: #13 (никаких неконтролируемых автоциклов/саморазмножения; внешнее размножение только по пошаговой команде).

## Сделано в этой сессии (реальные фиксы)
- [x] ПАМЯТЬ: починен octopus-memory-restore-alert.py (AttributeError: list.get) — критический алерт о восстановлении памяти снова работает (был сломан, падал каждые 10 мин).
- [x] ПАМЯТЬ: починен octopus-obsidian-vector-sync.py (KeyError ref в api_post) — векторная индексация знаний работала вхолостую (0% чанков индексировалось), теперь Failed chunks=0.
- [x] ЖИТЬ: 0 failed units, 0 сервисов с NRestarts>5, smoke 40/0/0.

---
## НЕЗАВИСИМЫЕ ПОТОКИ (можно вести параллельно)

### Поток A — ПАМЯТЬ (приоритет №1)
- [ ] A1. Дать vector-sync дорабатывать в фоне по таймеру до полного покрытия wiki (CPU-bound, не блокирует).
- [x] A2. Идемпотентность vector-sync (СДЕЛАНО: sha256-кэш): пропускать уже проиндексированные чанки (хэш текста) -> экономия CPU/нагрузки.
- [x] A3. 3-я копия памяти ВЫПОЛНЕНА через AWS EC2-ноду (reboot+swap, coverage 1.0, аудитор обновлён). Garage-вариант — отдельно. (помимо local+AWS S3): standalone Garage/receiver, БЕЗ смены replication_factor основного кластера. Только в отдельное change-window.

### Поток B — ЖИТЬ (приоритет №2)
- [x] B1. Telegram-алерты протестированы (доставлено): контролируемый тест доставки (по согласованию, чтобы не спамить).
- [x] B2. Load 5.2->0.67 (кэш эмбеддингов + оптимизация replicator): тюнинг частоты таймеров эмбеддингов; единичная chat-модель 0.5b уже стоит.
- [x] B3. fly-нода заархивирована (nodes_archive.json) (🔴 offline, памяти не покрывает) либо восстановить (flyctl не установлен).

### Поток C — УПРОЩЕНИЕ (приоритет №3)
- [x] C1. fly-нода убрана из активного registry (8/8 online); watchdog tcp_only багфикс (fly), уменьшить шум мониторинга.
- [ ] C2. Ревизия /opt/octopus-*.py: общие хелперы (load_env, tg, api_post) вынести в один модуль.
- [ ] C3. Документировать единый путь запуска детей (systemd) в COMPACT_CONTEXT (уже частично).

### Поток D — СОСУЩЕСТВОВАНИЕ (приоритет №4)
- [x] D1. ollama cgroups (2G/2.5G/300%) + стресс-тест OK 0.5b под cgroups-квотами (не падает ли под нагрузкой).
- [ ] D2. Проверить отсутствие конфликтов портов (PORT_AUDIT актуализировать).

### Поток E — РАЗМНОЖАТЬСЯ (ТОЛЬКО по пошаговой команде, #08/#09/#13)
- [~] E1. Согласие получено. Вместо НОВОЙ платной t2.micro (вышло бы за free 750ч) оживлена существующая нода. Новая платная — по отд. решению. — только после явного "Согласен на AWS" в текущей сессии.

### Поток F — РАЗВИВАТЬСЯ/УЧИТЬСЯ/МЕНЯТЬСЯ
- [ ] F1. Master-loop на безопасную реальную задачу (напр. оптимизация nginx Control Plane) с проверкой результата каждого шага.
- [ ] F2. Накопление experience-уроков (этой сессии — добавлено).

## Принцип параллельности
Потоки A2/B2/C/D независимы по файлам и сервисам -> можно вести параллельно.
Потоки A3/E требуют отдельного change-window и команды пользователя.
