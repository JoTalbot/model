# Octopus — План развития по ВСЕМ векторам (2026-05-30 v3)
Приоритет (инструкция #11): ПАМЯТЬ > ЖИТЬ > УПРОЩЕНИЕ > СОСУЩЕСТВОВАНИЕ > РАЗМНОЖАТЬСЯ/РАЗВИВАТЬСЯ/УЧИТЬСЯ/МЕНЯТЬСЯ.
Глобальная цель (инструкция #05): ПРОЦЕСС.

## Сделано (Дневной блок)
- Упрощение: бэкапы слиты в один скрипт `octopus-backup-manager.sh`. 
- Жить: Master Loop выведен в Next.js Admin UI для запуска автономных циклов прямо из браузера.
- Учиться: добавлены новые отчеты по саморазвитию агентов.

---
## НОВЫЙ БЛОК ЗАДАЧ (Вечерний цикл)

### 8. ПАМЯТЬ (приоритет №1)
- [ ] Оптимизация локального хранилища: проверить занимаемое место `/var/lib/octopus/memory_pool` и выполнить `octopus-memory-gc-dryrun.py`.
- [ ] Проверить логи `octopus-wiki-archivist.py`, чтобы убедиться, что знания не теряются.

### 2/ЖИТЬ (приоритет №2)
- [ ] Аудит `NRestarts`: просканировать логи на предмет служб, которые перезапускались более 5 раз за последние 3 часа.
- [ ] Проверить Telegram Alerts: протестировать отправку тестового сообщения через `octopus-alerts-tg.py`.

### 7/УПРОЩЕНИЕ (приоритет №3)
- [x] Добавить в Next.js Admin UI страницу или модалку "Редактор конфига" (Config Editor), чтобы править `/etc/octopus/config.yaml` без SSH.
- [x] Очистка устаревших контейнеров (docker system prune) для освобождения ресурсов.

### 6/СОСУЩЕСТВОВАНИЕ (приоритет №4)
- [ ] Провести стресс-тест локального агента `ollama` (qwen2.5:0.5b) в условиях жестких cgroups-квот, чтобы убедиться, что он не падает под нагрузкой.

### РАЗМНОЖАТЬСЯ (только по команде)
- [ ] Поднять новую бесплатную ноду AWS (Только если пользователь введет "Согласен на AWS").

### РАЗВИВАТЬСЯ / УЧИТЬСЯ / МЕНЯТЬСЯ
- [ ] Запустить `octopus-agent-loop` (Мастер-цикл) на реальную задачу: "Оптимизировать конфигурацию Nginx для Control Plane".
