# Roadmap: Углубление Мультимодальности и Надежности (v14) — 2026-06-11
Приоритеты #11: ПАМЯТЬ > ЖИТЬ > УПРОЩЕНИЕ > СОСУЩЕСТВОВАНИЕ.
Статус: Запущено выполнение (распараллелено).

## 4 НЕЗАВИСИМЫХ ПОТОКА (параллельно)

### ПОТОК A — AUDIO SEMANTIC SEARCH (МУЛЬТИМОДАЛЬНОСТЬ)
A1. Подключить имеющиеся аудио-транскрипции (Whisper/Telegram) к pgvector.
A2. Векторизовать старые расшифровки с помощью CLIP (text-to-vector).
A3. Обеспечить регулярную синхронизацию новых транскрипций.

### ПОТОК B — S3 GLACIER / DEEP ARCHIVE (DURABILITY)
B1. Создать скрипт для регулярного бэкапа `packstore` (`*.tar.zst` и `zstd.dict`) в удаленное S3-совместимое хранилище.
B2. Настроить systemd-timer для ночного бэкапа.

### ПОТОК C — ПРОЗРАЧНЫЙ ЖУРНАЛ АВТОНОМИИ (СОСУЩЕСТВОВАНИЕ)
C1. Разработать скрипт, собирающий все действия (изменение файлов, команд, timer-запусков) за сутки.
C2. Формировать `digest` и сохранять в `/var/lib/octopus/autonomy_journal/`.
C3. Это соответствует Инструкции #18 (Человек видит, что система делает сама).

### ПОТОК D — БАЗА ДАННЫХ И ПРОИЗВОДИТЕЛЬНОСТЬ (УПРОЩЕНИЕ/ЖИТЬ)
D1. Оптимизировать параметры PostgreSQL (`shared_buffers`, `maintenance_work_mem`) для ускорения работы с векторными индексами (HNSW).
D2. Проверить индексы.

