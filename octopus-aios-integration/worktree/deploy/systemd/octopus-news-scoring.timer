# Wave 5, item 3. Disabled by default — enable explicitly.
[Unit]
Description=Historical news scoring retry (every 30 min until quota allows)

[Timer]
OnCalendar=*:0/30
Persistent=false

[Install]
WantedBy=timers.target
