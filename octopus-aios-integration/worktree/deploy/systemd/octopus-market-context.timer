# Wave 5, item 3. Disabled by default — enable explicitly.
[Unit]
Description=Daily market context (F&G) collection

[Timer]
OnCalendar=*-*-* 06:00:00
Persistent=true

[Install]
WantedBy=timers.target
